
services:
  backend:
    build: "./server"
    ports:
      - 8000:8000
      - 8080:8080
    env_file:
      - ./server/env/.env
      - ./server/env/.env.production
      - ./server/env/.env.developement